{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",

    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@types/*": ["types/*"]
      // (you can keep others too, e.g. "@feature/*": ["backend/feature/*"])
    }
  }
}
